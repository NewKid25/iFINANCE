@model Group7_iFINANCEAPP.Models.MasterAccount

@{
    ViewBag.Title = "Edit";
}

<div class="container my-5">
    <div class="card shadow-sm rounded-4 p-5">
        <h2 class="text-center mb-4">Edit Master Account</h2>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger text-center mb-3" })

            <div class="mb-3">
                @Html.LabelFor(model => model.name, "Account Name", htmlAttributes: new { @class = "form-label" })
                @Html.EditorFor(model => model.name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.name, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(model => model.openingAmount, "Opening Amount", htmlAttributes: new { @class = "form-label" })
                @Html.EditorFor(model => model.openingAmount, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.openingAmount, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(model => model.GroupID, "Group Name", htmlAttributes: new { @class = "form-label" })
                @Html.DropDownListFor(model => model.GroupID, (SelectList)ViewBag.GroupID, "-- Select Group --", new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.GroupID, "", new { @class = "text-danger" })
            </div>

            <div class="mb-4">
                @Html.Label("ID", "Account ID (not editable)", htmlAttributes: new { @class = "form-label" })
                <p class="form-control-plaintext">@Model.ID</p>
                @Html.HiddenFor(model => model.ID)
                @Html.ValidationMessageFor(model => model.ID, "", new { @class = "text-danger" })
            </div>

            <div class="text-center">
                <input type="submit" value="Save" class="btn btn-primary btn-lg me-3" />
                @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary btn-lg" })
            </div>
        }
    </div>
</div>

<style>
    .btn-primary {
        background-color: #8058ac;
        border-color: #8058ac;
    }

    .btn-primary:hover {
        background-color: #7048a0;
        border-color: #7048a0;
    }
</style>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
