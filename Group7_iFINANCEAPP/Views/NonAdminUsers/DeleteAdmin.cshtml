@model Group7_iFINANCEAPP.Models.Administrator

@{
    ViewBag.Title = "Delete";
}

<div class="container my-5">
    <div class="card shadow-sm rounded-4 p-5">
        <h2 class="text-center mb-4">Confirm Deletion</h2>
        <h4 class="text-center text-danger mb-4">Are you sure you want to delete this user account?</h4>

        <dl class="row">
            <dt class="col-sm-4">Name</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.name)</dd>

            <dt class="col-sm-4">Date Hired</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.dateHired)</dd>

            <dt class="col-sm-4">Date Finished</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.dateFinished)</dd>
        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="text-center mt-4">
                <input type="submit" value="Delete" class="btn btn-danger btn-lg me-3" />
                @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary btn-lg" })
            </div>
        }
    </div>
</div>

<style>
    .btn-primary {
        background-color: #8058ac;
        border-color: #8058ac;
    }

    .btn-primary:hover {
        background-color: #7048a0;
        border-color: #7048a0;
    }
</style>
